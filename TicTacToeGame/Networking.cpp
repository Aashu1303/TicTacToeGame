#include "Networking.h"

void InitMultiplayer() {
    // Initialize networking (e.g., setup socket connection)
}

void SendMove(int row, int col) {
    // Send the move to the other player (over a network)
}

void ReceiveMove(int& row, int& col) {
    // Receive a move from the other player (over a network)
}

void CloseMultiplayer() {
    // Close the network connection
}
